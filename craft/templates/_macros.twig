
{% macro image( file, size, default_alt_text, class ) -%}
	<img{% if class|length %} class="{{ class }}"{% endif %} src="{{ file.getUrl( size ) }}" width="{{ file.getWidth( size ) }}" height="{{ file.getHeight( size ) }}" alt="{{ file.altText ? file.altText : default_alt_text }}">
{%- endmacro %}
